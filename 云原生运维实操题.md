# 云原生运维综合实操题

## 📋 题目概述

**场景描述：**
你是一家电商公司的运维工程师，需要将一个传统的单体应用迁移到云原生架构。该应用包含：
- 前端 Web 服务（Nginx + Vue.js）
- 后端 API 服务（Spring Boot）
- 数据库服务（MySQL）
- 缓存服务（Redis）

**任务目标：**
完成应用的容器化、编排部署、监控告警、日志收集和高可用配置。

---

## 🎯 实操任务清单

### 第一阶段：容器化改造（30分）

#### 任务 1.1：编写 Dockerfile
为以下服务编写优化的 Dockerfile：

**要求：**
1. 前端服务：使用多阶段构建，基础镜像选择 nginx:alpine
2. 后端服务：使用 OpenJDK，优化镜像大小
3. 所有镜像需要：
   - 使用非 root 用户运行
   - 添加健康检查
   - 优化镜像层数
   - 设置时区为 Asia/Shanghai

**提示文件结构：**
```
project/
├── frontend/
│   ├── Dockerfile
│   └── nginx.conf
├── backend/
│   ├── Dockerfile
│   └── app.jar
└── docker-compose.yml
```

#### 任务 1.2：编写 Docker Compose
编写 docker-compose.yml 文件，实现：
- 所有服务的编排
- 网络隔离（前端网络、后端网络、数据库网络）
- 数据持久化（MySQL、Redis）
- 环境变量配置
- 资源限制（CPU、内存）
- 健康检查配置

---

### 第二阶段：Kubernetes 部署（40分）

#### 任务 2.1：编写 K8s 资源清单

**需要创建的资源：**

1. **Namespace**
   - 创建 `ecommerce-prod` 命名空间

2. **ConfigMap**
   - 应用配置文件
   - Nginx 配置文件

3. **Secret**
   - 数据库密码
   - Redis 密码
   - 镜像仓库凭证

4. **Deployment**
   - 前端服务：3 副本，滚动更新策略
   - 后端服务：3 副本，配置就绪探针和存活探针
   - 配置资源请求和限制
   - 配置亲和性和反亲和性

5. **StatefulSet**
   - MySQL：单实例，持久化存储
   - Redis：3 节点集群模式

6. **Service**
   - 前端：ClusterIP
   - 后端：ClusterIP
   - MySQL：Headless Service
   - Redis：Headless Service

7. **Ingress**
   - 配置域名访问
   - 配置 HTTPS（使用 cert-manager）
   - 配置路径路由

8. **HPA（水平自动伸缩）**
   - 后端服务：基于 CPU 和内存自动扩缩容
   - 目标：CPU 70%，内存 80%
   - 最小副本：3，最大副本：10

9. **PV 和 PVC**
   - MySQL 数据持久化：10Gi
   - Redis 数据持久化：5Gi

#### 任务 2.2：部署策略
实现以下部署策略：
1. 蓝绿部署脚本
2. 金丝雀发布配置（使用 Istio 或 Nginx Ingress）
3. 回滚方案

---

### 第三阶段：监控告警（15分）

#### 任务 3.1：Prometheus + Grafana 监控

**要求：**
1. 部署 Prometheus Operator
2. 配置 ServiceMonitor 监控：
   - 应用指标（JVM、请求量、响应时间）
   - 容器指标（CPU、内存、网络）
   - MySQL 指标（连接数、慢查询）
   - Redis 指标（命中率、内存使用）

3. 配置告警规则：
   - Pod 重启次数 > 5 次/小时
   - CPU 使用率 > 80%
   - 内存使用率 > 85%
   - 接口响应时间 > 2s
   - 错误率 > 5%

4. Grafana Dashboard：
   - 创建应用监控大盘
   - 创建资源使用大盘

#### 任务 3.2：日志收集
使用 EFK（Elasticsearch + Fluentd + Kibana）或 Loki 实现：
1. 收集所有 Pod 日志
2. 日志分类和索引
3. 配置日志保留策略（7天）
4. 创建常用日志查询模板

---

### 第四阶段：高可用与灾备（10分）

#### 任务 4.1：高可用配置
1. MySQL 主从复制配置
2. Redis 哨兵模式或集群模式
3. 配置 Pod 反亲和性，确保副本分散在不同节点
4. 配置 PodDisruptionBudget（PDB）

#### 任务 4.2：备份方案
1. 编写 MySQL 定时备份脚本（CronJob）
2. 备份文件上传到对象存储（MinIO/S3）
3. 编写恢复脚本和测试流程

---

### 第五阶段：CI/CD 流水线（5分）

#### 任务 5.1：Jenkins Pipeline
编写 Jenkinsfile 实现：
1. 代码拉取
2. 单元测试
3. 构建 Docker 镜像
4. 推送到镜像仓库
5. 更新 K8s Deployment
6. 健康检查
7. 自动回滚（如果健康检查失败）

---

## 📝 交付物清单

### 必须提交的文件：

```
cloud-native-ops/
├── docker/
│   ├── frontend/
│   │   ├── Dockerfile
│   │   └── nginx.conf
│   ├── backend/
│   │   └── Dockerfile
│   └── docker-compose.yml
│
├── kubernetes/
│   ├── namespace.yaml
│   ├── configmap.yaml
│   ├── secret.yaml
│   ├── frontend-deployment.yaml
│   ├── backend-deployment.yaml
│   ├── mysql-statefulset.yaml
│   ├── redis-statefulset.yaml
│   ├── services.yaml
│   ├── ingress.yaml
│   ├── hpa.yaml
│   ├── pv-pvc.yaml
│   └── pdb.yaml
│
├── monitoring/
│   ├── prometheus/
│   │   ├── prometheus-config.yaml
│   │   └── alert-rules.yaml
│   ├── grafana/
│   │   └── dashboard.json
│   └── servicemonitor.yaml
│
├── logging/
│   └── fluentd-config.yaml
│
├── backup/
│   ├── mysql-backup-cronjob.yaml
│   └── backup-script.sh
│
├── cicd/
│   └── Jenkinsfile
│
└── scripts/
    ├── deploy.sh
    ├── rollback.sh
    └── health-check.sh
```

---

## 🎓 评分标准

| 项目 | 分值 | 评分要点 |
|------|------|----------|
| Dockerfile 编写 | 15分 | 多阶段构建、安全性、优化 |
| Docker Compose | 15分 | 网络、存储、资源限制 |
| K8s 资源清单 | 25分 | 完整性、规范性、最佳实践 |
| 监控告警 | 15分 | 指标覆盖、告警合理性 |
| 高可用配置 | 10分 | 架构设计、容错能力 |
| CI/CD 流水线 | 10分 | 自动化程度、回滚机制 |
| 文档说明 | 10分 | 部署文档、运维手册 |

---

## 💡 加分项

1. 使用 Helm Chart 管理应用（+5分）
2. 实现 GitOps（ArgoCD/FluxCD）（+5分）
3. 配置服务网格（Istio/Linkerd）（+5分）
4. 实现混沌工程测试（Chaos Mesh）（+3分）
5. 配置成本优化方案（+2分）

---

## ⏰ 时间要求

- 建议完成时间：4-6 小时
- 可分阶段完成，每个阶段独立评分

---

## 🔧 环境准备

### 本地环境：
- Docker 20.10+
- Docker Compose 2.0+
- kubectl 1.24+
- Helm 3.0+

### 集群环境：
- Kubernetes 1.24+（可使用 Minikube/Kind/K3s）
- 至少 3 个节点（1 Master + 2 Worker）
- 每个节点：4C8G

---

## 📚 参考资源

- [Docker 官方文档](https://docs.docker.com/)
- [Kubernetes 官方文档](https://kubernetes.io/docs/)
- [Prometheus 文档](https://prometheus.io/docs/)
- [Helm 文档](https://helm.sh/docs/)

---

## ❓ 常见问题

**Q1: 可以使用云服务商的托管 K8s 吗？**
A: 可以，但需要说明使用的服务和配置。

**Q2: 数据库可以使用云数据库吗？**
A: 建议自建，以展示 StatefulSet 和持久化能力。

**Q3: 监控组件必须自己部署吗？**
A: 是的，需要展示完整的监控栈部署能力。

---

## 🎯 实战建议

1. **先本地后集群**：先用 Docker Compose 验证，再迁移到 K8s
2. **逐步推进**：不要一次性完成所有任务，分阶段验证
3. **注重细节**：资源限制、健康检查、日志输出等细节很重要
4. **文档完善**：每个配置都要有注释说明
5. **测试验证**：每个功能都要测试验证，截图记录

---

## 📖 提交说明

1. 将所有文件整理到 `cloud-native-ops` 目录
2. 编写 `README.md` 说明部署步骤
3. 提供测试截图和验证结果
4. 说明遇到的问题和解决方案

---

**祝你实操顺利！💪**
