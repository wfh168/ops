# Ubuntu 22.04 配置静态IP地址 10.0.0.91

## 方法一：使用Netplan配置（推荐）

### 1. 查看当前网络接口
```bash
# 查看网络接口名称
ip addr show
# 或者
ip link show

# 常见接口名：eth0, ens33, enp0s3等
```

### 2. 备份现有配置
```bash
sudo cp /etc/netplan/00-installer-config.yaml /etc/netplan/00-installer-config.yaml.backup
```

### 3. 编辑Netplan配置文件
```bash
sudo vim /etc/netplan/00-installer-config.yaml
```

### 4. 配置静态IP（根据你的网络环境调整）
```yaml
# This is the network config written by 'subiquity'
network:
  ethernets:
    ens33:  # 替换为你的实际网卡名称
      dhcp4: false
      addresses:
        - 10.0.0.91/24
      routes:
        - to: default
          via: 10.0.0.1  # 网关地址，根据实际情况修改
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
          # 或使用国内DNS
          # - 114.114.114.114
          # - 223.5.5.5
  version: 2
```

### 5. 应用配置
```bash
# 测试配置文件语法
sudo netplan try

# 如果测试通过，应用配置
sudo netplan apply
```

### 6. 验证配置
```bash
# 查看IP地址
ip addr show

# 测试网络连通性
ping -c 4 10.0.0.1  # ping网关
ping -c 4 8.8.8.8   # ping外网DNS
```

## 方法二：临时配置（重启后失效）

### 临时设置IP地址
```bash
# 设置IP地址（临时）
sudo ip addr add 10.0.0.91/24 dev ens33

# 设置默认网关（临时）
sudo ip route add default via 10.0.0.1

# 设置DNS（临时）
echo "nameserver 8.8.8.8" | sudo tee /etc/resolv.conf
```

## 常见网络环境配置示例

### 示例1：家庭网络环境
```yaml
network:
  ethernets:
    ens33:
      dhcp4: false
      addresses:
        - 10.0.0.91/24
      routes:
        - to: default
          via: 10.0.0.1
      nameservers:
        addresses:
          - 192.168.1.1  # 路由器IP
          - 8.8.8.8
  version: 2
```

### 示例2：企业网络环境
```yaml
network:
  ethernets:
    ens33:
      dhcp4: false
      addresses:
        - 10.0.0.91/24
      routes:
        - to: default
          via: 10.0.0.1
      nameservers:
        addresses:
          - 10.0.0.1      # 内网DNS
          - 114.114.114.114
  version: 2
```

### 示例3：虚拟机环境（VMware/VirtualBox）
```yaml
network:
  ethernets:
    ens33:  # VMware通常是ens33
      dhcp4: false
      addresses:
        - 10.0.0.91/24
      routes:
        - to: default
          via: 10.0.0.2   # 虚拟机网关通常是.2
      nameservers:
        addresses:
          - 8.8.8.8
          - 8.8.4.4
  version: 2
```

## 故障排除

### 1. 检查网卡名称
```bash
# 如果不确定网卡名称
ls /sys/class/net/
# 或者
networkctl list
```

### 2. 检查配置文件语法
```bash
# 检查YAML语法
sudo netplan --debug try
```

### 3. 重启网络服务
```bash
# 重启网络服务
sudo systemctl restart systemd-networkd

# 或者重启网络管理器
sudo systemctl restart NetworkManager
```

### 4. 查看详细错误信息
```bash
# 查看网络状态
sudo networkctl status

# 查看系统日志
sudo journalctl -u systemd-networkd
```

## 完整配置脚本

创建一个自动配置脚本：

```bash
#!/bin/bash
# 配置静态IP脚本

# 设置变量
INTERFACE="ens33"  # 修改为你的网卡名称
IP_ADDRESS="10.0.0.91/24"
GATEWAY="10.0.0.1"
DNS1="8.8.8.8"
DNS2="8.8.4.4"

# 备份原配置
sudo cp /etc/netplan/00-installer-config.yaml /etc/netplan/00-installer-config.yaml.backup

# 创建新配置
sudo tee /etc/netplan/00-installer-config.yaml << EOF
network:
  ethernets:
    $INTERFACE:
      dhcp4: false
      addresses:
        - $IP_ADDRESS
      routes:
        - to: default
          via: $GATEWAY
      nameservers:
        addresses:
          - $DNS1
          - $DNS2
  version: 2
EOF

# 应用配置
echo "正在应用网络配置..."
sudo netplan apply

# 验证配置
echo "当前IP配置："
ip addr show $INTERFACE | grep inet

echo "测试网络连通性："
ping -c 2 $GATEWAY
```

## 注意事项

1. **网卡名称**：不同系统网卡名称可能不同（eth0, ens33, enp0s3等）
2. **网关地址**：确保网关地址正确，通常是网段的第一个或第二个IP
3. **子网掩码**：/24表示255.255.255.0，根据实际网络调整
4. **DNS服务器**：可以使用路由器IP、公共DNS或企业内部DNS
5. **防火墙**：配置完成后检查防火墙规则是否影响网络访问

## 验证清单

配置完成后，检查以下项目：

- [ ] IP地址设置正确：`ip addr show`
- [ ] 网关可达：`ping 10.0.0.1`
- [ ] DNS解析正常：`nslookup google.com`
- [ ] 外网连通：`ping 8.8.8.8`
- [ ] SSH服务可用：`sudo systemctl status ssh`
- [ ] 配置持久化：重启后IP不变