# 应届生运维工程师实操题

## 📋 考试说明

**适用对象：** 应届生/初中级运维工程师  
**考试时长：** 2-3 小时  
**考试形式：** 上机实操 + 文档输出  
**总分：** 100 分（60分及格）

---

## 🎯 场景描述

你刚入职一家互联网公司，负责维护一个简单的 Web 应用。该应用包含：
- 一个 Nginx Web 服务器
- 一个 MySQL 数据库
- 一些定时任务脚本

现在需要你完成以下运维任务。

---

## 第一部分：Linux 基础操作（25分）

### 任务 1.1：系统信息查看（5分）

编写一个脚本 `system_info.sh`，输出以下信息：
- 操作系统版本
- CPU 核心数和型号
- 内存总量和使用情况
- 磁盘使用情况
- 当前登录用户数

**要求：**
- 输出格式清晰易读
- 使用颜色区分不同信息（可选加分项）

### 任务 1.2：日志分析（8分）

给定一个 Nginx 访问日志文件 `access.log`，完成以下分析：

1. 统计访问量最多的前 10 个 IP 地址
2. 统计访问量最多的前 10 个 URL
3. 统计各个 HTTP 状态码的数量
4. 找出响应时间超过 1 秒的请求

**提示：** 使用 grep、awk、sed 等工具

### 任务 1.3：文件管理（7分）

1. 创建目录结构：
```
/data/webapp/
├── logs/
├── backup/
├── scripts/
└── config/
```

2. 设置权限：
   - logs 目录：所有人可读写，但只有 owner 可删除文件
   - backup 目录：只有 root 和 backup 组可访问
   - scripts 目录：所有脚本文件自动具有执行权限

3. 创建一个软链接 `/var/log/webapp` 指向 `/data/webapp/logs`

### 任务 1.4：进程管理（5分）

1. 查找占用 CPU 最高的 5 个进程
2. 查找占用内存最高的 5 个进程
3. 编写命令杀死所有名为 `test_process` 的进程
4. 查看 8080 端口被哪个进程占用

---

## 第二部分：Shell 脚本编程（20分）

### 任务 2.1：备份脚本（10分）

编写一个 MySQL 数据库备份脚本 `mysql_backup.sh`，要求：

**功能：**
- 备份指定数据库
- 备份文件命名格式：`dbname_YYYYMMDD_HHMMSS.sql.gz`
- 保存到 `/data/webapp/backup/` 目录
- 自动删除 7 天前的备份文件
- 记录备份日志到 `/data/webapp/logs/backup.log`

**参数：**
- `-h`: 数据库主机（默认 localhost）
- `-u`: 数据库用户
- `-p`: 数据库密码
- `-d`: 数据库名称

**示例：**
```bash
./mysql_backup.sh -h localhost -u root -p password -d mydb
```

### 任务 2.2：服务监控脚本（10分）

编写一个服务监控脚本 `service_monitor.sh`，要求：

**功能：**
- 检查 Nginx、MySQL 服务是否运行
- 检查 80、3306 端口是否监听
- 检查磁盘使用率是否超过 80%
- 检查内存使用率是否超过 85%
- 如果发现异常，发送告警（写入日志文件）

**输出格式：**
```
[2024-02-09 10:30:00] [OK] Nginx 服务运行正常
[2024-02-09 10:30:00] [OK] MySQL 服务运行正常
[2024-02-09 10:30:00] [WARNING] 磁盘使用率 85%，超过阈值
[2024-02-09 10:30:00] [OK] 内存使用率 60%
```

---

## 第三部分：Web 服务配置（20分）

### 任务 3.1：Nginx 配置（12分）

配置 Nginx 实现以下需求：

1. **虚拟主机配置**
   - 域名：`www.example.com`
   - 网站根目录：`/data/webapp/html`
   - 默认首页：`index.html`

2. **反向代理配置**
   - 路径 `/api` 代理到后端服务 `http://127.0.0.1:8080`
   - 添加必要的请求头（X-Real-IP、X-Forwarded-For）

3. **静态资源优化**
   - 图片、CSS、JS 文件启用 gzip 压缩
   - 静态文件缓存 7 天

4. **日志配置**
   - 访问日志：`/data/webapp/logs/access.log`
   - 错误日志：`/data/webapp/logs/error.log`
   - 日志格式包含：IP、时间、请求方法、URL、状态码、响应时间

5. **安全配置**
   - 隐藏 Nginx 版本号
   - 限制请求体大小为 10M
   - 禁止访问 `.git` 目录

### 任务 3.2：测试验证（8分）

1. 使用 `curl` 命令测试配置是否生效
2. 编写测试脚本验证：
   - 网站是否可访问
   - 反向代理是否正常
   - gzip 压缩是否启用
   - 错误页面是否正确返回

---

## 第四部分：数据库管理（15分）

### 任务 4.1：MySQL 基础操作（8分）

1. 创建数据库 `webapp_db`，字符集 `utf8mb4`
2. 创建用户 `webapp_user`，密码 `WebApp@2024`
3. 授予该用户对 `webapp_db` 的所有权限
4. 创建表 `users`：
```sql
CREATE TABLE users (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL UNIQUE,
    email VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_username (username)
);
```

5. 插入 3 条测试数据
6. 编写查询语句：查找最近 7 天创建的用户

### 任务 4.2：数据库优化（7分）

1. 查看 MySQL 当前连接数
2. 查看慢查询日志配置
3. 编写命令导出 `webapp_db` 数据库
4. 编写命令从备份文件恢复数据库
5. 说明如何查看表的索引使用情况

---

## 第五部分：定时任务（10分）

### 任务 5.1：Crontab 配置（10分）

配置以下定时任务：

1. 每天凌晨 2 点执行数据库备份脚本
2. 每小时执行一次服务监控脚本
3. 每天凌晨 3 点清理 7 天前的日志文件
4. 每周日凌晨 4 点重启 Nginx 服务
5. 每 5 分钟检查磁盘空间，如果使用率超过 90% 发送告警

**要求：**
- 所有任务的输出重定向到对应的日志文件
- 确保脚本路径使用绝对路径

---

## 第六部分：故障排查（10分）

### 场景 6.1：网站无法访问（5分）

**现象：** 用户反馈网站无法访问

**任务：** 写出你的排查思路和步骤（至少 5 个步骤）

### 场景 6.2：服务器负载高（5分）

**现象：** 服务器负载突然升高，响应缓慢

**任务：** 
1. 列出你会使用的排查命令（至少 5 个）
2. 说明每个命令的作用
3. 描述可能的原因和解决方案

---

## 附加题：Docker 基础（10分，加分项）

### 任务 7.1：容器化部署（10分）

1. 编写 Dockerfile 将一个简单的 Web 应用容器化
2. 使用 docker-compose 编排 Nginx + MySQL 服务
3. 配置数据持久化
4. 配置容器自动重启

---

## 📦 交付物清单

请将以下文件整理到一个文件夹中提交：

```
submission/
├── part1/
│   ├── system_info.sh
│   ├── log_analysis.txt (分析结果)
│   └── commands.txt (任务1.3和1.4的命令)
├── part2/
│   ├── mysql_backup.sh
│   └── service_monitor.sh
├── part3/
│   ├── nginx.conf
│   └── test_script.sh
├── part4/
│   ├── database_setup.sql
│   └── commands.txt (数据库操作命令)
├── part5/
│   └── crontab.txt (定时任务配置)
├── part6/
│   └── troubleshooting.md (故障排查文档)
└── README.md (说明文档)
```

---

## 📊 评分标准

| 部分 | 分值 | 评分要点 |
|------|------|----------|
| Linux 基础 | 25分 | 命令使用正确性、输出格式 |
| Shell 脚本 | 20分 | 功能完整性、代码规范、错误处理 |
| Web 服务 | 20分 | 配置正确性、安全性考虑 |
| 数据库管理 | 15分 | SQL 语法、权限管理 |
| 定时任务 | 10分 | Crontab 语法、任务合理性 |
| 故障排查 | 10分 | 思路清晰、方法正确 |
| Docker（加分） | 10分 | 配置正确、最佳实践 |

---

## 💡 评分细则

### 优秀（90-100分）
- 所有任务完成且正确
- 代码规范，有注释
- 考虑了错误处理和边界情况
- 有安全意识
- 完成加分项

### 良好（75-89分）
- 主要任务完成
- 代码基本规范
- 有基本的错误处理
- 配置基本正确

### 及格（60-74分）
- 完成 60% 以上的任务
- 基本功能可用
- 有明显的改进空间

### 不及格（<60分）
- 完成任务少于 60%
- 存在严重错误
- 基础知识不扎实

---

## 📚 考试环境

### 系统环境
- 操作系统：CentOS 7 / Ubuntu 20.04
- Nginx：1.20+
- MySQL：5.7+ / 8.0+
- Docker：20.10+（可选）

### 提供的测试数据
- `access.log`：Nginx 访问日志样本（1000行）
- `test_db.sql`：测试数据库脚本

---

## ⏰ 时间分配建议

- 第一部分（Linux 基础）：30 分钟
- 第二部分（Shell 脚本）：40 分钟
- 第三部分（Web 服务）：30 分钟
- 第四部分（数据库）：25 分钟
- 第五部分（定时任务）：15 分钟
- 第六部分（故障排查）：15 分钟
- 检查和整理：15 分钟

---

## 📝 注意事项

1. **安全性**：所有脚本中的密码不要硬编码，使用参数传递或配置文件
2. **日志记录**：重要操作要记录日志，包含时间戳
3. **错误处理**：脚本要有基本的错误处理机制
4. **代码规范**：变量命名清晰，适当添加注释
5. **测试验证**：完成后要测试验证功能是否正常

---

## 🎓 考察重点

- **Linux 基础命令**：文件操作、权限管理、进程管理
- **Shell 编程能力**：逻辑控制、函数使用、错误处理
- **服务配置能力**：Nginx、MySQL 的基本配置
- **问题排查能力**：日志分析、性能排查
- **运维意识**：安全、备份、监控、自动化

---

## 💪 备考建议

1. 熟练掌握 Linux 三剑客（grep、sed、awk）
2. 熟悉 Shell 脚本基本语法和常用技巧
3. 了解 Nginx 和 MySQL 的基本配置
4. 练习日志分析和故障排查
5. 掌握 Crontab 定时任务配置
6. 了解基本的性能监控命令（top、free、df、netstat）

---

**祝你考试顺利！加油！💪**
